interface:
  name: /api/fail_safe/state
  type: autoware_ad_api_msgs/msg/FailSafeState
  method: notification

description: |-
  Get the fail-safe state. For details, see the [fail-safe state](./index.md).

message:
  - name: state
    text: A value of the [fail-safe state](./index.md).
  - name: auto_recovery
    text: Whether the fail-safe state recovers automatically.
