interface:
  name: /api/motion/state
  type: autoware_ad_api_msgs/msg/MotionState
  method: notification

description: |-
  Get the motion state. For details, see the [motion state](/features/motion.md).

message:
  - name: state
    text: A value of the [motion state](/features/motion.md).
