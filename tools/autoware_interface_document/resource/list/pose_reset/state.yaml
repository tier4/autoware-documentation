interface:
  name: /api/pose_reset/state
  type: autoware_ad_api_msgs/msg/PoseResetState
  method: notification

description: |-
  Get the pose reset state. For details, see the [pose reset state](./index.md).

message:
  - name: state
    text: A value of the [pose reset state](./index.md).
